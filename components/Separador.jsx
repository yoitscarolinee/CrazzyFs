import styled from "styled-components";

const Separador = () => {
    return ( <Hr /> );
}
 
const Hr = styled.hr`

    width: 100%;
    border: 0;
    height: 1px;
    background-color: rgba(0, 0, 0, 0.1);
    margin: 30px 0;

`;

export default Separador;